# MedAI Hub - Backend Dependencies
# FastAPI and ASGI Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Supabase Client
supabase>=2.11.0
postgrest>=0.17.0

# Google Gemini AI via LangChain
langchain-google-genai>=2.0.5
langchain>=0.3.0
langchain-core>=0.3.0

# Pydantic for data validation (compatible with Python 3.14)
pydantic>=2.10.0
pydantic-settings>=2.6.0

# HTTP Client
httpx>=0.28.0
aiofiles>=24.1.0

# Encoding detection and retry logic
chardet>=5.2.0
tenacity>=8.2.0

# Environment variables
python-dotenv>=1.0.1

# Date handling
python-dateutil>=2.9.0

# Rate limiting
slowapi>=0.1.9

# Caching (Redis is optional - uses in-memory cache by default)
redis>=5.0.0  # Optional: Install for distributed caching with REDIS_URL

# Testing (optional but recommended)
pytest>=8.3.0
pytest-asyncio>=0.24.0
